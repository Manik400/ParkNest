{
  "_comment": "The hosted Staging environment (docs/hosting-back4app-neon.md). Only settings that are the same for every Staging deploy and are not secrets live here, so the host needs just the secrets as environment variables. Storage:LocalRoot 'media' already resolves to /app/media in the image, and Messaging, Push and Payments:Provider keep their defaults (InProcess, None, None).",
  "Database": {
    "MigrateOnStartup": true
  },
  "Email": {
    "Provider": "Smtp"
  },
  "Cache": {
    "Provider": "Memory"
  }
}
